<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.github.davidricardo1026</groupId>
        <artifactId>easytrans-demo</artifactId>
        <version>2.0.0</version>
    </parent>

    <artifactId>easytrans-demo-both</artifactId>
    <name>easytrans-demo-both</name>
    <description>Test module for both package configs (Full override priority)</description>

    <dependencies>
        <dependency>
            <groupId>io.github.davidricardo1026</groupId>
            <artifactId>easytrans-core</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${lombok.version}</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
                <configuration>
                    <source>17</source>
                    <target>17</target>
                    <compilerArgs>
                        <!-- 同时配置后缀和全包名，测试全包名优先级高于后缀 -->
                        <arg>-Aeasytrans.generated.package.suffix=customgen.registry</arg>
                        <arg>-Aeasytrans.generated.bridge.package.suffix=customgen.bridge</arg>
                        <arg>-Aeasytrans.generated.package=io.github.easytrans.test.both.registry</arg>
                        <arg>-Aeasytrans.generated.bridge.package=io.github.easytrans.test.both.bridge</arg>
                        <arg>-Aeasytrans.enable.spring=false</arg>
                    </compilerArgs>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                        <path>
                            <groupId>io.github.davidricardo1026</groupId>
                            <artifactId>easytrans-processor</artifactId>
                            <version>${project.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
